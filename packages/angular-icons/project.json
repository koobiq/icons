{
    "name": "@koobiq/angular-icons",
    "$schema": "../../node_modules/nx/schemas/project.schema.json",
    "targets": {
        "generate": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npx tsx scripts/generate.ts && eslint . --fix",
                "cwd": "{projectRoot}",
                "assets": ["{workspaceRoot}/packages/icons/svg/**/*.svg"]
            },
            "inputs": ["{workspaceRoot}/packages/icons/svg/**/*.svg"],
            "outputs": ["{projectRoot}/icons", "{projectRoot}/public-api.ts"]
        },
        "build": {
            "executor": "@nx/angular:package",
            "dependsOn": ["generate"],
            "options": {
                "project": "{projectRoot}/ng-package.json"
            },
            "outputs": ["{workspaceRoot}/dist/packages/{projectName}"]
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "options": {
                "eslintConfig": "{projectRoot}/.eslintrc.js",
                "lintFilePatterns": ["{projectRoot}/**/*.ts"],
                "ignorePath": "{workspaceRoot}/.eslintignore",
                "hasTypeAwareRules": true
            }
        },
        "prepublish": {
            "executor": "nx:run-commands",
            "dependsOn": ["build"],
            "options": {
                "command": "npx tsx scripts/copy-meta-to-dist.ts",
                "cwd": "{projectRoot}"
            },
            "inputs": ["{workspaceRoot}/LICENSE", "{projectRoot}/README.md"],
            "outputs": [
                "{workspaceRoot}/dist/packages/{projectName}/LICENSE",
                "{workspaceRoot}/dist/packages/{projectName}/README.md"
            ]
        },
        "nx-release-publish": {
            "dependsOn": ["prepublish"],
            "options": {
                "packageRoot": "{workspaceRoot}/dist/packages/{projectName}"
            }
        }
    }
}
